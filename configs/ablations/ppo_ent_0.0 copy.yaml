# PPO axis C — entropy coefficient = 0 (탐험 보너스 제거).
# 효과: ent_coef 0.01 → 0.0 → 정책이 더 빨리 결정론적으로 수렴.
#       조기 수렴/local optimum 위험↑. Breakout 처럼 탐험이 중요한 과제에서
#       탐험 보너스의 기여를 측정하는 대조군.
# 근거: 기존 ppo_ent_0.05 와 합쳐 ent_coef 스윕(0.0·0.01·0.05)을 구성한다.
inherits: ../ppo.yaml

algo:
  kwargs:
    ent_coef: 0.0
