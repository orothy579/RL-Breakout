# PPO axis A — even wider trust region.
# 효과: clip 효과 약화 → vanilla policy gradient 에 가까워짐. divergence 위험↑.
inherits: ../ppo.yaml

algo:
  kwargs:
    clip_range: 0.3

train:
  total_timesteps: 50000000
  checkpoint_freq: 5000000
  eval_freq: 5000000
  n_eval_episodes: 3